# OpenSpace core dependencies
litellm>=1.83.7,<2,!=1.82.7,!=1.82.8  # excludes compromised 1.82.7/1.82.8 and includes the >=1.83.7 SQLi fix
python-dotenv>=1.0.0
openai>=1.0.0
jsonschema>=4.25.0
bashlex>=0.18
mcp>=1.0.0
anthropic>=0.71.0
pillow>=12.0.0
numpy>=1.24.0
colorama>=0.4.6

# Local server dependencies (cross-platform)
flask>=3.1.0
pyautogui>=0.9.54
pydantic>=2.12.0
aiohttp>=3.10.0
requests>=2.32.0
websockets>=13.0

# # macOS-specific dependencies (local server)
# pyobjc-core>=12.0; sys_platform == 'darwin'
# pyobjc-framework-cocoa>=12.0; sys_platform == 'darwin'
# pyobjc-framework-quartz>=12.0; sys_platform == 'darwin'

# # Linux-specific dependencies (local server)
# python-xlib>=0.33; sys_platform == 'linux'
# pyatspi is installed by the Linux distribution (for Debian/Ubuntu:
# python3-pyatspi); the similarly named PyPI package is obsolete.

# # Windows-specific dependencies (local server)
# pywinauto>=0.6.8; sys_platform == 'win32'
# pywin32>=306; sys_platform == 'win32'
# PyGetWindow>=0.0.9; sys_platform == 'win32'
